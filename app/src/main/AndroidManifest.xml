<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="jp.ac.asojuku.st.noffication_de_study">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/NoActionBar_NoMultiTup">
        <receiver android:name=".LocalNotificationReceiver"/>
        <receiver android:name=".LocalNotificationTwoQuestionScheduleService"/>
        <activity android:name=".TitleActivity">
        </activity>
        <activity android:name=".QuestionActivity">
        </activity>
        <activity android:name=".QuestionOptionActivity">
        </activity>
        <activity android:name=".AnswerActivity">
        </activity>
        <activity android:name=".OptionActivity">
        </activity>
        <activity android:name=".StaticsActivity">
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>

</manifest>